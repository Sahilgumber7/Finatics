import { createContext } from "react";

export const balance = createContext(0);
export const expense = createContext(0);
export const income = createContext(0);
export const username = createContext("");
export const time = createContext();